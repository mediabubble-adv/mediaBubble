/** Default `sizes` for responsive grid/card images (Next.js Image). */
export const DEFAULT_RESPONSIVE_IMAGE_SIZES =
  '(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw'

/** Hero / full-bleed images above the fold. */
export const HERO_IMAGE_SIZES = '100vw'

/** Narrow sidebar or avatar thumbnails. */
export const THUMBNAIL_IMAGE_SIZES = '(max-width: 640px) 20vw, 96px'
